//Question 7

// model type
ctmc

// constants
const A0 = 15;
const double rateDegrad = 0.1;

module degradation 
	X_ : [0..A0] init A0;

	[degrad]  (X_ > 0)  -> (rateDegrad) : (X_' = X_ - 1);
endmodule


//------------------------------------------------------------------------------------------------
//Question 9 
//------------------------------------------------------------------------------------------------

// model type
ctmc

// constants
const A0 = 15;
const double rateDegrad = 0.1;

module degradation 
	X_ : [0..A0] init A0;

	[degrad]  (X_>=2)  -> (rateDegrad) : (X_' = X_ - 2);
endmodule

//-------------------------------------------------------------------------------------------------
//Question 10
//-------------------------------------------------------------------------------------------------
//Ajout d'un reaction rate et d'une constante compartment

// File generated by automatic SBML-to-PRISM conversion
// Original SBML file: sbml1.prism.tmp.xml

ctmc

const int MAX_AMOUNT = 100;

// Compartment size
const double compartment = 1.0;

// Parameters for reaction reaction_1
const double k1 = 0.1; // k1

// Species A (A)
const int _A_MAX = MAX_AMOUNT;
module _A
	
	_A : [0.._A_MAX] init 15; // Initial amount 15
	
	// reaction_1 (reaction_1)
	[reaction_1] _A > 0 -> (_A'=_A-1);
	
endmodule

// Reaction rates
module reaction_rates

	// reaction_1 (reaction_1): A -> 
	[reaction_1] (compartment*k1*_A) > 0 -> (compartment*k1*_A) : true;

endmodule

// Reward structures (one per species)

// Reward 1: A (A)
rewards "_A" true : _A; endrewards


//----------------------------------------------------------------------------------------------------
// Question  11
//----------------------------------------------------------------------------------------------------

// Oui on obtient les mêmes résultats.

//-----------------------------------------------------------------------------------------------------
// Question 12
//-----------------------------------------------------------------------------------------------------

